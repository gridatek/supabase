# Environment Configuration for Supabase Scripts
# Copy this file to .env.local: cp .env.example .env.local

# Environment type (leave empty or set to "production" for production deployments)
ENVIRONMENT=

# Supabase project reference (required for production deployments)
# Get this from your Supabase dashboard: Settings > General > Reference ID
PROJECT_ID=

# Note: For local development, you don't need to set these variables.
# The scripts will default to local mode and use the Docker Compose setup.
